# Common credentials shared across all environments
# Copy to common.enc.env and encrypt with: sops -e -i credentials/common.enc.env
#
# These are tied to your OVH account, not a specific project.

# OVH API
TF_VAR_endpoint=ovh-eu
TF_VAR_application_key=your_application_key
TF_VAR_application_secret=your_application_secret
TF_VAR_consumer_key=your_consumer_key

# SSH
TF_VAR_public_key=ssh-ed25519 AAAA... your@email
TF_VAR_private_key=~/.ssh/id_ed25519

# S3 credentials for Terraform state backend (OVH Object Storage, S3-compatible)
# These are generated automatically by: make bootstrap ENV=<env>
# After bootstrap, retrieve them with:
#   cd terraform/bootstrap && terraform output -raw access_key_id
#   cd terraform/bootstrap && terraform output -raw secret_access_key
# Then paste the values here and re-encrypt: sops -e -i credentials/common.enc.env
AWS_ACCESS_KEY_ID=generated_by_bootstrap
AWS_SECRET_ACCESS_KEY=generated_by_bootstrap

# Docker Hub (private registry for API image)
DOCKERHUB_USERNAME=your_dockerhub_username
DOCKERHUB_TOKEN=your_dockerhub_access_token
