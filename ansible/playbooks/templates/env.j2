# Auto-generated by Ansible — do not edit manually

# Node
NODE_ENV={{ node_env }}
PORT={{ api_port }}

# Database (OVH managed PostgreSQL)
DATABASE_URL=postgresql://{{ db_user }}:{{ db_password }}@{{ db_host }}:{{ db_port }}/{{ db_name }}?sslmode=require

# JWT RS256
JWT_PRIVATE_KEY_PATH=/app/keys/private.pem
JWT_PUBLIC_KEY_PATH=/app/keys/public.pem
JWT_EXPIRES_IN={{ jwt_expires_in }}

# PowerSync
PS_PORT={{ powersync_port }}
PS_DATA_SOURCE_URI=postgresql://{{ db_user }}:{{ db_password }}@{{ db_host }}:{{ db_port }}/{{ db_name }}?sslmode=require
PS_STORAGE_URI=postgresql://{{ db_user }}:{{ db_password }}@{{ db_host }}:{{ db_port }}/{{ db_powersync_name }}?sslmode=require
PS_JWKS_URL=http://api:{{ api_port }}/.well-known/jwks.json
